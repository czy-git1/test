<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.dao.mapper.FavoriteMapper">
  <resultMap id="BaseResultMap" type="com.mall.dao.model.Favorite">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="favorite_id" jdbcType="INTEGER" property="favoriteId" />
    <result column="user_id" jdbcType="INTEGER" property="userId" />
    <result column="product_id" jdbcType="INTEGER" property="productId" />
    <association property="product" column="product_id" select="com.mall.dao.mapper.ProductMapper.selectProductById"/>
  </resultMap>

  <select id="selectMyFav" resultMap="BaseResultMap">
    select * from favorite fav where fav.user_id=#{userId} limit #{start}, #{end}
  </select>

  <select id="selectMyFavCount" resultType="java.lang.Integer">
    select count(*) from favorite fav where fav.user_id=#{userId}
  </select>

</mapper>