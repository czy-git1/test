<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.dao.mapper.TOrderMapper">
  <resultMap id="BaseResultMap" type="com.mall.dao.model.TOrder">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="order_id" jdbcType="INTEGER" property="orderId" />
    <result column="order_code" jdbcType="VARCHAR" property="orderCode" />
    <result column="user_id" jdbcType="INTEGER" property="userId" />
    <result column="order_time" jdbcType="TIMESTAMP" property="orderTime" />
    <result column="total_price" jdbcType="DOUBLE" property="totalPrice" />
    <result column="receive_name" jdbcType="VARCHAR" property="receiveName" />
    <result column="full_address" jdbcType="VARCHAR" property="fullAddress" />
    <result column="mobile" jdbcType="VARCHAR" property="mobile" />
    <result column="status" jdbcType="INTEGER" property="status" />
    <result column="exp_no" jdbcType="VARCHAR" property="expNo" />
    <result column="remark" jdbcType="LONGVARCHAR" property="remark" />
    <collection property="orderItems" column="order_id" select="com.mall.dao.mapper.OrderItemMapper.selectOrderItemByOrderId">
    </collection>
  </resultMap>

  <select id="selectOrderByUserId" resultMap="BaseResultMap">
    select * from t_order t where t.user_id=#{userId} order by t.order_time desc limit #{start}, #{end}
  </select>
  <select id="selectOrderCountByUserId" resultType="java.lang.Integer">
    select count(*) from t_order t where t.user_id=#{userId}
  </select>

  <select id="selectOrderById" resultMap="BaseResultMap">
    select * from t_order t where t.order_id=#{orderId}
  </select>

  <select id="selectOrderList" resultMap="BaseResultMap">
    select * from t_order t where 1=1
    <if test="kw!=null and kw!=''">
      AND t.order_code LIKE CONCAT('%',#{kw},'%')
    </if>
    order by t.order_time desc limit #{start}, #{end}
  </select>
  <select id="selectOrderCount" resultType="java.lang.Integer">
    select count(*) from t_order t where 1=1
    <if test="kw!=null and kw!=''">
      AND t.order_code LIKE CONCAT('%',#{kw},'%')
    </if>
  </select>

  <!--订单日期统计曲线图数据-->
  <select id="selectOrderDateCount" resultType="java.util.Map">
    SELECT DATE(oi.order_time) as odate,COUNT(oi.order_id) as total FROM t_order oi
    WHERE oi.order_time &gt;= DATE(#{startDate}) AND oi.order_time &lt;= DATE(#{endDate}) GROUP BY odate
  </select>

  <select id="selectOrderAmountCount" resultType="java.util.Map">
    SELECT DATE(oi.order_time) as odate,SUM(oi.total_price) as total FROM t_order oi
    WHERE oi.order_time &gt;= DATE(#{startDate}) AND oi.order_time &lt;= DATE(#{endDate}) GROUP BY odate
  </select>
</mapper>