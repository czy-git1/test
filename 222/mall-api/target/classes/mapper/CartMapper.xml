<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.dao.mapper.CartMapper">
  <resultMap id="BaseResultMap" type="com.mall.dao.model.Cart">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="cart_id" jdbcType="INTEGER" property="cartId" />
    <result column="product_id" jdbcType="INTEGER" property="productId" />
    <result column="quantity" jdbcType="INTEGER" property="quantity" />
    <result column="user_id" jdbcType="INTEGER" property="userId" />
    <result column="is_select" jdbcType="INTEGER" property="isSelect" />
  </resultMap>

  <update id="selectAllItem">
    update cart c set c.is_select=1 where c.user_id=#{userId}
  </update>

  <update id="disSelectAll">
    update cart c set c.is_select=0 where c.user_id=#{userId}
  </update>

  <update id="selectCart">
    update cart c set c.is_select=#{isSelect} where c.cart_id=#{cartId}
  </update>

  <select id="selectCartByUserSelected" resultMap="BaseResultMap">
    select * from cart c where c.user_id=#{userId} and c.is_select=1
  </select>

</mapper>