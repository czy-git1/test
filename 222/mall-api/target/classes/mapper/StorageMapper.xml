<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.dao.mapper.StorageMapper">
  <resultMap id="BaseResultMap" type="com.mall.dao.model.Storage">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="storage_id" jdbcType="INTEGER" property="storageId" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
    <result column="product_id" jdbcType="INTEGER" property="productId" />
    <result column="in_out" jdbcType="INTEGER" property="inOut" />
    <result column="quantity" jdbcType="INTEGER" property="quantity" />
    <result column="remark" jdbcType="VARCHAR" property="remark" />
    <result column="new_quantity" jdbcType="INTEGER" property="newQuantity" />
    <association property="product" column="product_id" select="com.mall.dao.mapper.ProductMapper.selectProductById"/>
  </resultMap>

  <select id="selectLastStorageByProductId" resultMap="BaseResultMap">
    select * from storage st where st.product_id=#{productId} order by st.storage_id desc limit 0,1
  </select>

  <select id="selectStorageList" resultMap="BaseResultMap">
    select st.* from storage st where 1=1
    <if test="kw!=null and kw!=''">
      and st.product_id IN(select p.product_id from product p where p.name LIKE CONCAT('%',#{kw},'%'))
    </if>
    order by st.create_time desc limit #{start},#{end}
  </select>

  <select id="selectStorageCount" resultType="java.lang.Integer">
    select count(*) from storage st where 1=1
    <if test="kw!=null and kw!=''">
      and st.product_id IN(select p.product_id from product p where p.name LIKE CONCAT('%',#{kw},'%'))
    </if>
  </select>
</mapper>